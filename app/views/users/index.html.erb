<p style="color: green"><%= notice %></p>

<h1>Buscar Usuários</h1>

<%= form_tag(users_path, method: :get) do %>
  <%= text_field_tag 'search', nil, placeholder: 'Digite o termo de busca...' %>
  <%= submit_tag 'Buscar' %>
<% end %>

<h1>Users</h1>

<div id="users">
  <% @users.each do |user| %>
    <%= render user %>
    <p>
      <%= link_to "Show this user", user %>
    </p>
  <% end %>
</div>

<%= link_to "New user", new_user_path %>
